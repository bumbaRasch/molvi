{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "Settings window: core:event:allow-listen + allow-unlisten for mic-level/navigate-history (the Microphone section unlistens on cleanup); core:app:default for getVersion() in Updates/About; opener:allow-open-url scoped to the About-section credit domains.",
  "windows": ["settings"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:app:default",
    {
      "identifier": "opener:allow-open-url",
      "allow": [
        { "url": "https://github.com/*" },
        { "url": "https://huggingface.co/*" }
      ]
    }
  ]
}
